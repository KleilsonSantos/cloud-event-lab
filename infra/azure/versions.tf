terraform {
  required_version = ">= 1.6.0"

  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "~> 4.0"
    }
  }
}

provider "azurerm" {
  features {}
  # REAL_CLOUD only — no Azurite defaults in provider block.
}

# Phase 5 skeleton: no managed resources yet.
