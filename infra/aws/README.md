# OpenTofu / Terraform — AWS

Skeleton for Phase 5. Provider: hashicorp/aws (or OpenTofu equivalent).

LocalStack endpoint overrides belong in env / backend config — document carefully so REAL_CLOUD apply is never accidental.
