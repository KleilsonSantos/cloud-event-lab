# Shared modules

Only for genuinely shared pieces (naming, tags). Do not force a fake multi-cloud module (ADR-007).

Phase 5 ships **empty** shared modules on purpose — cloud trees under `infra/aws|azure|gcp` stay separate.
