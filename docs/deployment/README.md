# Deployment (public vs local)

## Local lab (full)

Compose + profiles + emulators. See README root.

## Public demo (future)

Must be **cheap/simple**. Do **not** put LocalStack/Azurite/gcloud emulators on the public internet as “production multi-cloud”.

Candidates (revalidate pricing/ToS on deploy date — **NÃO VALIDADO** until chosen):

- Cloudflare Pages / Workers
- GitHub Pages (static docs/UI)
- Render / Fly.io free tiers for slim API

LIVE DEMO link only when a real public instance exists.
