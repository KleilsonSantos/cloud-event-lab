# Azure profile — Blob + Queue via Azurite (LOCAL EMULATOR).
spring:
  config:
    activate:
      on-profile: azure

lab:
  cloud:
    provider: azure
  azure:
    # Expands to the public Azurite devstoreaccount1 connection (see AzureBlobObjectStorageAdapter).
    storage-connection: UseDevelopmentStorage=true
    blob:
      container: cloud-event-lab
      endpoint: http://127.0.0.1:10000/devstoreaccount1
    queue:
      endpoint: http://127.0.0.1:10001/devstoreaccount1
      visibility-seconds: 30
