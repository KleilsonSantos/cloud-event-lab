# Azure profile — Blob via Azurite (LOCAL EMULATOR). Message bus still needs profile `local` until Phase 3.
spring:
  config:
    activate:
      on-profile: azure

lab:
  cloud:
    provider: azure
  azure:
    # Host default ports (10000–10002). Prefer the explicit string when Azurite runs in Docker.
    storage-connection: DefaultEndpointsProtocol=http;AccountName=devstoreaccount1;AccountKey=Eby8vdM02xNOcqFlqUwJPLlmEtlCDXJ1OUzFT50uSRZ6IFsuFq2UVErCz4I6tq/K1SZFPTOtr/KBHBeksoGMGw==;BlobEndpoint=http://127.0.0.1:10000/devstoreaccount1;
    blob:
      container: cloud-event-lab
